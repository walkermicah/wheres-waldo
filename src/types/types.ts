export type Target = {
  name: string;
  imgUrl: string;
  found: boolean;
};

export type Score = {
  id: string;
  name: string;
  time: number;
};

export type ClickCoords = {
  x: number;
  y: number;
};
